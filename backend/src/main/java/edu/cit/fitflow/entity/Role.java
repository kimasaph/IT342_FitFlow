package edu.cit.fitflow.entity;

/**
 * Enum representing the different roles a user can have in the system.
 */
public enum Role {
    ADMIN,   // Full access to the system, including user management
    MEMBER,  // Limited access to personal features
    TRAINER  // Access to training-related features
}
